#include<stdio.h>
int main()
{ int a;
  printf("enter the values of a");
  scanf("%d",&a);
  if(a%2==0) 
  { printf("a is positive");
  }
  else
  { printf("a is negative");
  }
  return(0);
}
